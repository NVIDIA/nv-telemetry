[package]
name = "nv-telemetry-schema"
description = "Protobuf contract for nv-telemetry, and its compiled descriptor set"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

# The schema is compiled by protox at build time, so no protoc binary is
# needed here or anywhere downstream. Nothing is pulled into the runtime: the
# crate exposes the descriptor set as bytes.
[build-dependencies]
protox.workspace = true

[lints]
workspace = true
