[package]
name = "nv-telemetry-model"
description = "Observation data plane for nv-telemetry, generated from the schema"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
prost.workspace = true

[dev-dependencies]
# The fidelity tests compare the generated types against the descriptors the
# invariant rules ran over. Depending on the schema crate rather than the
# compiler keeps the dependency pointing one way.
nv-telemetry-schema.workspace = true
prost-reflect.workspace = true

gungraun.workspace = true

[[bench]]
name = "model"
harness = false

[lints]
workspace = true
